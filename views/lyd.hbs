<center>
    <h1 class="loggedInHeading">Lyde</h1>
    <p class="loggedInText">Her kan du optage lyde og tilføje til din skraldespand.</p>
</center>

<div class="container">
    <div class="row">
        <div class="col-md-6 m-auto">
            <form action="/lyd/audioupload" method="POST" enctype="multipart/form-data">
                <div class="custom-file mb-3">
                    Optag video: <input type="file" accept="audio/*" id="capture" capture="audio" name="file" class="custom-file-input">
                    <label for="file" class="custom-file-label">Vælg lydfil</label>
                </div>
                <input type="submit" value="Tilføj audio" class="btn btn-primary btn-block">
            </form>
            <hr>
            {{#if files}}
            {{#each files}}
            <div class="card card-body mb-3">
                <audio controls>
                         <source src="lyd/audio/{{filename}}" type="audio/mp4">Din browser understøtter ikke denne filtype!</audio>
                <form method="PUT" action="/lyd/audio/{{_id}}.mp4?_method=PUT"></form>
                <form method="POST" action="/lyd/files/go/{{_id}}">
                    <button class="btn btn-danger btn-block mt-4">Slet lyd</button>
                </form>
            </div>
            {{/each}}
            {{/if}}
        </div>
    </div>
</div>